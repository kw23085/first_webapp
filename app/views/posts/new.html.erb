<h1>Add A Post</h1>

<%= form_for @post do |p| %>
    <%= p.text_field(:title, placeholder: "Title") %>
    <%= p.text_field(:picture_url, placeholder: "Picture URL") %>
    <%= p.text_field(:body, placeholder: "What you want to say") %>
    <br>
    <%= p.submit("Add Post") %>
    
<% end %>